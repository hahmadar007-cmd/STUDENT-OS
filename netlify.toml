# netlify.toml
# Automates Netlify deployment settings for the monorepo

[build]
  base = "frontend"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NEXT_PUBLIC_API_URL = "https://ammeeee-student-os.hf.space"

[[plugins]]
  package = "@netlify/plugin-nextjs"
